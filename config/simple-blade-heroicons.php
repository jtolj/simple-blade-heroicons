<?php

return [
   /*
    |-----------------------------------------------------------------
    | Default to use references
    |-----------------------------------------------------------------
    |
    | This config option allows you to define the default for the
    | `push-refs` property option. If set to true, the definition for
    | the icon will be pushed to template and a reference will be used
    | instead of inlining the entire SVG.
    | 
    | See README.md to properly configure the `simple-blade-icons` stack
    | in your template.
    |
    */
    'use_references' => false,
];